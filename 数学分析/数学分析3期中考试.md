# 一
在 $\mathbb{R}^{3}$ 中，令 $\mathbf{F} = (yz+ax,xz+2y,xy+2z)$

$\mathbf{F}$ 是有势场。
## 1
求 $a$
## 2
求一个势函数。
## 3
令 $\mathbf{r}=(\cos t,\sin t,t)$ 计算从 $(1,0,0)$ 到 $(-1,0,\pi)$，即 $t$ 从 $0$ 到 $\pi$ 的积分 $\int_{C}\mathbf{F}\cdot\mathrm{d}\mathbf{r}$

---
# 二
计算收敛半径
## 1
 $\sum\limits_{ n=1 }^{ \infty }(-1)^{n}nx^{n}$
## 2
$\sum\limits_{ n=1 }^{ \infty }\frac{n+1}{3^{n}}x^{2n+1}$
## 3
$\sum\limits_{ n=1 }^{ \infty }\frac{n!}{n^{n}}x^{n}$

---
# 三

判断级数绝对收敛、条件收敛或发散
## 1
$\sum\limits_{ n=1 }^{ \infty } (-1)^{n}\frac{1}{n+5}$

## 2
$\sum\limits_{ n=1 }^{ \infty }\left( 1-(-1)^{n}\cos\left( \frac{1}{n} \right) \right)$

## 3
$\sum\limits_{ n=1 }^{ \infty }(-1)^{n} \frac{1}{\sqrt{ n }+(-1)^{n}}$

---
# 四
若 $\sum\limits_{ n=1 }^{ \infty }a_{n}x^{n}$ 的收敛半径为 $R_{1}$, $\sum\limits_{ n=1 }^{ \infty } b_{n}x^{n}$ 的收敛半径为 $R_{2}$. $\sum\limits_{ n=1 }^{ \infty } (a_{n}-b_{n})x^{n}$ 的收敛半径为 $R$

## 1
证明：$R \geq \mathrm{min}(R_{1},R_{2})$
## 2
举例说明存在 $a_{n},b_{n}$ 使得 $R>\mathrm{min}(R_{1},R_{2})$

---
# 五
已知 $\sum\limits_{ n=1 }^{ \infty }a_{n}$ 收敛。讨论

$$
\begin{align}
\sum\limits_{ n=1 }^{ \infty } \frac{na_{n}}{(1+a_{1})\dots(1+a_{n} )} 
\end{align}
$$

的收敛性。

---

# 六
函数列$\{ f_{n}(x) \}:f_{n}(x)=\frac{x}{1+n^{2}x^{2}}$

## 1
$\sum\limits_{ n=1 }^{ \infty }f_{n}(x)$ 在 $(0,+\infty)$ 上不一致收敛。

## 2
$\forall\delta>0,\sum\limits_{ n=1 }^{ \infty }f_{n}(x)$ 在 $[\delta,+\infty)$ 上一致收敛。

# 七
证明：
$f(x)=\sum\limits_{ n=1 }^{ \infty }\frac{\cos (nx)}{n^{3}}$ 在 $(-\infty,+\infty)$ 上连续可微。

# 八

函数列 $\{ f_{n}(x) \}f_{n}(x)$ 在 $[a,b]$ 上单调

$\{ f_{n}(x) \}$ 在 $x=a$ 和 $x = b$ 处绝对收敛。
证明:

$$
\begin{align}
\sum\limits_{ n=1 }^{ \infty } f_{n} (x)
\end{align}
$$

在 $[a,b]$ 上一致且绝对收敛。









