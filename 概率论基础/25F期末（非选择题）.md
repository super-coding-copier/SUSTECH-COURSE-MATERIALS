# 1（10分）
如果下蛋的数量服从参数为 $\lambda$ 的 Poisson 分布，每个蛋孵化是独立事件，概率是 $p$, 求孵化成功的个数的概率质量函数和分布类型。


---

# 2（20分）

## 1
如果 $Z\sim N(0,1)$, 那么对于任何有界连续函数，

$$
E(f'(Z))=E(Zf(Z))
$$

## 2
若 $X,Y,Z$ i.i.d, 求

$$
E(X+Y|X+Y+Z)
$$

## 3
$X_{1},X_{2},\dots X_{m}\sim N(\mu,1)$, $Y_{1},Y_{2},\dots,Y_{n}\sim N(\mu,4)$ 这些随机变量是相互独立的。
如果

$$
T=a\sum\limits_{i=1}^{m} X_{i} +b\sum\limits_{i=1}^{n} Y_{i}
$$
 
 如果 $E(T)=\mu$，求 $a,b$ 的取值，使得 $Var(T)$ 最小。


 ---

 # 3 (20分）

 $(X,Y)$ 是在区域 $x^{2}+y^{2} \leq 1$ 上的均匀分布

## 1
求 $X,Y$ 的边缘密度函数

## 2
判断 $X,Y$ 是否独立，并说明理由。

## 3
令 $U=\sqrt{ X^{2}+Y^{2} }$ 求 $E(U)$

## 4
求 $U=u(0<u<1)$ 条件下 $x$ 的条件分布

## 5
求 $E(X^{2}|U=0.5)$

---

# 4 （20分）

如果 $X_{1},X_{2},\dots,X_{n},\dots$ 是独立同分布的，它们的累积分布函数是 $F(x)$

## 1
陈述弱大数定律

## 2
陈述中心极限定理

## 3 
令 $Y_{n}(x) = \sum \limits_{i=1}^{n} I(X_{i} \leq x)$

求

$$
a(x) = \lim_{ n \to \infty } \frac{Y_{n}}{n}
$$
 

## 4
令

$$
Z_{n}(x)=\sqrt{ n }( \frac{Y_{n}}{n}-F(x) )
$$

求 $Z_{n}(x)$ 的极限，以及找到 $\varepsilon(x)$, 使得

$$P( \bigg| \frac{Y_{n}}{n}-F(x) \bigg| < \varepsilon (x) )\approx 0.95$$
 






